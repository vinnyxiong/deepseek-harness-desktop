const assert=require('node:assert/strict');const test=require('node:test');const {LocaleService}=require('../src/main/locale-service');test('reads DSH locale preference and emits changes',async()=>{let changed;const service=new LocaleService({systemLanguages:['zh-CN'],callApi:async()=>({namespaces:[{ns:'locale',value:{preference:'en'}}]})});service.on('change',v=>{changed=v});await service.setEndpoint('http://127.0.0.1:1');assert.equal(service.getLocale(),'en');assert.equal(changed,'en')});test('refreshes only for locale settings events',async()=>{let calls=0;const service=new LocaleService({callApi:async()=>{calls++;return{namespaces:[]}}});await service.setEndpoint('x');service.handleHostFrame({type:'host/remote-event',event:'settings/document-updated',args:['other',1]});await new Promise(r=>setImmediate(r));assert.equal(calls,1);service.handleHostFrame({type:'host/remote-event',event:'settings/document-updated',args:['locale',2]});await new Promise(r=>setImmediate(r));assert.equal(calls,2)});
