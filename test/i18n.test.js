const assert=require('node:assert/strict');const test=require('node:test');const {DICTIONARIES,detectSystemLocale,translate}=require('../src/main/i18n');test('locale dictionaries have matching keys',()=>assert.deepEqual(Object.keys(DICTIONARIES.en).sort(),Object.keys(DICTIONARIES.zh).sort()));test('detects supported system locale with zh fallback',()=>{assert.equal(detectSystemLocale(['en-US']),'en');assert.equal(detectSystemLocale(['fr-FR']),'zh')});test('translates menu labels',()=>assert.equal(translate('zh','menu.connection'),'连接'));
